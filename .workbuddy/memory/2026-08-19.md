# 2026-08-19

## 打包 v2.0.1-pre.1 并发布到 GitHub
- 重写 CHANGELOG 顶部 `[2.0.1-pre.1]`，按 `v2.0.0-pre.1..HEAD` 真实 diff 核对（仅 `cfg-` 配置迁移、powerPlan 高亮修复、ApplyCpuAsync、sync/import、switch-stability-test.ps1、useControlState 重构+单测），剔除误写的 Toast/备份/更新/平台控制等。
- 一键脚本 `installer/build-installer.ps1 -Version 2.0.1` 跑通：产出 `dist/installer/DouzhanzheConsole-2.0.1-Setup.exe`（9.3MB）。
- 提交 `c05c997`（changelog）→ `git push origin feature/v2.0` → `git tag -a v2.0.1-pre.1` → `git push origin v2.0.1-pre.1`，均已上 GitHub。
- 踩坑记录：本环境沙箱会回滚 git 写入、Node 安全删除拦截批量删除、PowerShell 安全删除拦截 Remove-Item；用户关闭沙箱+调高批量删除阈值+关闭回收站保护后解决。sync-repos 不会 push 已提交未推的提交，需手动 push 分支和 tag（已写入 MEMORY.md）。
