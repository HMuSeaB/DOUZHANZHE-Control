# 2026-09-26

## 第十六轮：「再看看」—— 复查 fix.8 后的残留同类缺陷 + 文档对账

用户看过 fix.8 交付后说「再看看」。做了一次横向复查，结论：**代码层还剩 2 处同类残留，但都判定为「不该动」**。

### 复查方法与结果

把 `SavePerfOverrides` 全部 15 处调用点按端点归位，逐一确认「动硬件前是否已校验 mode」：

| 行号 | 端点 | 状态 |
|---|---|---|
| 1082 | `/api/control` | ✅ 已加守卫（fix.8） |
| 1338–1341 | `/api/smu/set` | ✅ 已加守卫 |
| 1402 | `/api/fan/set-target` | ✅ 唯一接住返回值的（fix.7） |
| 1487 | `/api/fan/restore` | ✅ 已加守卫 |
| 1654 | `/api/gpu/set` | ✅ 已加守卫 |
| 1747/1757/1768 | nvapi ×3 | ✅ 已加守卫 |
| 1799/1817/1835/1852 | cpu ×4 | ✅ 已加守卫 |
| **1888** | `/api/overrides/switch` | ⚠️ 在 `if (profileData.HasValue)` 分支内 → mode 必然有效，**安全** |
| **1928** | `/api/overrides/switch`（cpuResetTask） | ⚠️ 用 `req.Mode`，属 switch 语义，见下 |
| **1966** | `/api/overrides/clear` | ⚠️ **忽略返回值**，见下 |

### 两处残留 + 为什么不改

**1. `/api/overrides/clear` 忽略 `SavePerfOverrides` 返回值**（`Program.cs:1966`）
未知 mode 时落盘静默失败，但硬件清理照跑，最后返回 `ok:true`（假报成功）。
**不改的理由**：`mode` 解析是 `IsNullOrWhiteSpace(req.Mode) ? CurrentMode() : req.Mode`。
若用户曾把「当前模式」设成一个后来被删除的自定义配置，前端面板的「恢复默认」会带上这个
失效 mode → 一旦硬拒绝（400），`clearOverrides()` 里 `if (!res.ok) throw` 会直接抛错，
**反而把原本能降级工作的按钮变成报错**。收益（只有非法调用者能触发）远小于回归风险。

**2. `/api/overrides/switch` 完全不校验 mode**（`Program.cs:1869`）
一进来就 `SetCurrentMode(req.Mode)` + `ApplyThermalMode(...)`，未知 mode 会把 `CurrentMode()`
污染成非法值（此后所有 `SavePerfOverrides(..., null)` 都会失败）。
**不改的理由**：`switchMode()` 在**应用启动时**就会被调用（`useControlState.js:227`，
用持久化的当前模式），且同样 `if (!res.ok) throw` 且无 catch。
硬拒绝会让「持久化模式指向已删除配置」的用户**开不了应用**。当前的优雅降级是对的。

→ 结论：这两处是**有意的容错差异**，不是漏修。已确认前端所有调用点（`clearOverride`/
`switchMode`）传的都是合法 `settings.mode`，正常路径不受影响。
若将来要收紧，正确做法是**让失败可见而非拒绝**（记日志 + 响应带 `persisted:false`），
且必须在同一版里同步处理前端 `throw`。

### 顺带确认的两件事

- **自动切档链路是安全的**：`ProcessMonitorService.GetEffectiveMode()` → WebSocket
  `{type:"auto_switch", mode}` → 前端 `useControlState.js:260` → `switchMode(mode)`。
  传的是**配置 id**（与 `_currentConfig()` 同型比较），`IsBuiltinConfig` 能识别，
  所以 fix.8 的 11 处守卫不会误伤它。
- **回归脚本实为 9 组断言**（`[1]`–`[8]`，含 `[3b]`），而 `AGENTS.md:97` 写「5 项」、
  `tools/README.md:37` 写「7 项」，两处都过期。**已改成「9 组断言式回归测试」**。
  这是 fix.8 加 `[8]` 时漏改的文档。

### 另注

- `dist/installer/_superseded/` 目录已不存在（fix.7 旧包未留存）。`dist/` 在 `.gitignore` 内，
  无影响，但**别再依赖那个目录做归档**。
- 用户当前安装版仍是 **fix.6**；fix.7 / fix.8 两个包已生成但**尚未安装**。
